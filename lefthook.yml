pre-commit:
  parallel: false
  jobs:
    - name: oxfmt
      glob: "*.{ts,tsx,js,mjs}"
      run: npx oxfmt {staged_files}
      stage_fixed: true
    - name: oxlint
      glob: "*.{ts,tsx,js,mjs}"
      run: npx oxlint {staged_files}
