pre-commit:
  parallel: false
  jobs:
    - name: oxfmt
      glob: "*.{ts,tsx,js,mjs}"
      run: pnpm exec oxfmt {staged_files}
      stage_fixed: true
    - name: oxlint
      glob: "*.{ts,tsx,js,mjs}"
      run: pnpm exec oxlint {staged_files}
